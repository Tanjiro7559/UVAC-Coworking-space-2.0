{
  "version": 2,
  "buildCommand": "node vercel-build.js",
  "outputDirectory": ".vercel/output",
  "regions": ["sin1"],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api"
    },
    {
      "src": "/(.*)",
      "dest": "/static/$1"
    }
  ]
}
